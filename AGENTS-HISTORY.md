# AGENTS-HISTORY

## 2026-04-26 — Initial project setup

- Created Express.js backend (`server.js`) proxying Telegram Bot API `sendMessage`.
- Created frontend (`public/index.html`) with text editor, parse_mode selector, web preview toggle.
- Added `.env` for BOT_TOKEN/CHAT_ID, `.env.example` for reference, `.gitignore`.
- Repository created at https://github.com/wildcar/tg-ui-sandbox and pushed.
- Agent documentation updated (TECHSPEC, SUMMARY, HISTORY).
